import React from 'react';
import { useTranslation } from 'react-i18next';

function Pay() {
    const { t } = useTranslation();

    return (
        <section className="Pay-container-wrapper">

        </section>
    );
}

export default Pay;
